apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus   # dashboards/grafana.json panels bind to this uid
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
