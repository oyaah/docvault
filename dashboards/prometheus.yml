global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: docvault
    static_configs:
      - targets: ["docvault:8000"]
    metrics_path: /api/metrics
